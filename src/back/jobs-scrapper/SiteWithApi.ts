/* import Site from './Site';

abstract class SiteWithApi implements Site {
    abstract name: string;
    abstract address: string;
    abstract logoImage: string;
    abstract endpointAddress: string;
} */